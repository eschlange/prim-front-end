<header id="header" class="header red-ribbon">

    <div class="modal custom fade" id="loginModal">
      <div class="modal-header">
        <button class="close" data-dismiss="modal">×</button>
        <h3>Sign In</h3>
      </div>
      <div class="modal-body">
        <%= simple_form_for(:user, :url => "/sites/1/users/sign_in", :method => 'post') do |f| %>
        <div class="inputs">
          <%= f.input :email, :required => false, :autofocus => true %>
          <%= f.input :password, :required => false %>
          <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
        </div>
      </div>
      <div class="modal-footer">
        <%= f.button :submit, "sign in", :class => 'btn btn-primary' %>
      <% end %>
      </div>
    </div>

  <!-- Modal Danger -->
  <div class="modal fade modal-hmcf modal-danger" id="modalDanger" tabindex="-1" role="dialog" aria-labelledby="modalDangerLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true"><i class="fa fa-times-circle-o"></i></button>
          <div class="row">
            <div class="col-sm-4">
              <i class="fa fa-comments icon"></i>
            </div> <!-- /.col-sm-6 -->
            <div class="col-sm-8">
              <h4 class="modal-title" id="modalDangerLabel">Drop us a line</h4>
              <p>Have any questions or concerns?</p>
              <address>
                <strong>Division of Infectious Diseases</strong><br>
                <strong>Feinberg School of Medicine</strong><br>
                Steven Wolinsky, MD<br>
                645 N Michigan Ave, Suite 900<br>
                Chicago, IL 60666<br>
                <abbr title="Phone">P:</abbr> (312) 695-5088<br>
                <a href="mailto:s-galvin@northwestern.edu" target="_top">s-galvin@northwestern.edu</a>
              </address>
            </div> <!-- /.col-sm-6 -->
          </div> <!-- /.row -->
        </div> <!-- /.modal-header -->
      </div> <!-- /.modal-content -->
    </div> <!-- /.modal-dialog -->
  </div> <!-- /.modal -->

    <nav class="navbar navbar-default navbar-static-top" role="navigation">
      <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navabr-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'MACs', (site_path(@site) + "/pages/home"), :class => "navbar-brand" %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navabr-1">
          <ul class="nav navbar-nav">
            <li><%= link_to 'about us', (site_path(@site) + "/pages/home#about") %></li>
            <li><%= link_to 'eligibility', (site_path(@site) + "/pages/home#eligibility") %></li>
            <li><%= link_to 'resources', (site_path(@site) + "/pages/home#resources") %></li>
            <li><%= link_to 'contact us', (site_path(@site) + "/pages/home#contact") %></li>
            <% if !user_signed_in? %>
                <%= render "shared/sign_up_navbar" %>
            <% else %>
                <%= render "shared/authorized_navbar" %>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container -->
    </nav>

    <!-- Homepage navabr - it shows up on scroll -->
    <nav class="navbar navbar-default navbar-scroll navbar-fixed-top hidden" data-top-offset="500" role="navigation">
      <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navabr-2">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'MACs', (site_path(@site) + "/pages/home"), :class => "navbar-brand" %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse single-page-nav" id="navabr-2">
          <ul class="nav navbar-nav ">
            <li><a href="#about">about us</a></li>
            <li><a href="#eligibility">eligibility</a></li>
            <li><a href="#resources">resources</a></li>
            <li><a href="#contact">contact us</a></li>
            <% if !user_signed_in? %>
              <%= render "shared/sign_up_navbar" %>
            <% else %>
              <%= render "shared/authorized_navbar" %>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container -->
    </nav>

  <% if 'home' == @current_page %>
  <div class="container banner">
    <div class="row">
      <% if current_user.nil? %>
          <div class="col-sm-9">
            <div class="header-description">
              <h1 class="title red-ribbon-title">MACs : Multicenter AIDS Cohort Study Program<br>the first and largest study specifically created to examine the natural history of AIDS. </h1>
            </div><!-- /header-description -->
          </div><!-- /col-sm-9-->
          <div class="col-sm-3">
            <div class="app-store-button-wrapper app-store-button-wrapper-custom">
              <a class="btn btn-warning btn-large apple-version banner-btn" href="/sites/1/users/sign_up">Enroll Here</a>
            </div>
          </div>
      <% else %>
          <div class="header-description">
            <h1 class="title red-ribbon-title">MACs : Multicenter AIDS Cohort Study Program<br>the first and largest study specifically created to examine the natural history of AIDS. </h1>
          </div><!-- /header-description -->
      <% end %>
    </div><!-- /row -->
  </div><!-- /container -->

  <% end %>

  <% if 'devise/registrations' == params[:controller] %>
  <div class="container registration-header">
    <div class="header-description pull-left">
      <h1 class="title">MACs Enrollment </h1>
    </div><!-- /header-description -->
  </div><!-- /container -->
  <% end %>

</header>