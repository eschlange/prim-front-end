<!DOCTYPE html>
<% if @site.nil?
        @site = Site.find(1)
   end
%>
<html lang="en">
    <head>
      <title>PRIM</title>
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <%= stylesheet_link_tag    "simplemap", media: "all", "data-turbolinks-track" => true %>
      <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
      <%= csrf_meta_tags %>
    </head>

    <body>
      <%= render partial: 'pages/' + @site.template_name + '/shared/header' %></div>
      <%= yield %>
      <%= render partial: 'pages/' + @site.template_name + '/shared/footer' %>
    </body>
</html>